# Ficher apellé seulement par bookdown::bs4_book

# example R options set globally
options(width = 60)

# example chunk options set globally
knitr::opts_chunk$set(
  comment = "#>",
  collapse = TRUE,
  dpi = 144,
  fig.align = "center"
  )
